dependencies:
  # TODO(lzuttermeister): Unwanted dependency. Refactor code so that we can remove it.
  # (merge with use_case subpack?)
  - /lib/src/use_case
  # TODO(lzuttermeister): Unwanted dependency. Refactor code so that we can remove it.
  - /lib/src/addon_config
  # TODO(lzuttermeister): Unwanted dependency. Refactor code so that we can remove it.
  - /lib/src/_internal/src/use_case
