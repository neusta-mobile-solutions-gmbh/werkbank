dependencies:
  # TODO(lzuttermeister): Unwanted dependency. Refactor code so that we can remove it.
  - /lib/src/use_case
  # TODO(lzuttermeister): Unwanted dependency. Refactor code so that we can remove it.
  - /lib/src/tree
  # TODO(lzuttermeister): Unwanted dependency. Refactor code so that we can remove it.
  - /lib/src/addons
  # TODO(lzuttermeister): Unwanted dependency. Refactor code so that we can remove it.
  - /lib/src/use_case
  # TODO(lzuttermeister): Unwanted dependency. Refactor code so that we can remove it.
  - /lib/src/components
  # TODO(lzuttermeister): Unwanted dependency. Refactor code so that we can remove it.
  - /lib/src/use_case_metadata
